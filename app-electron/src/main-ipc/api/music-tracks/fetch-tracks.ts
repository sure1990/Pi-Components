const fetchTracks = async (musicId: number) => {
  const tracks: any[] = [];
  try {
    //Some Code
  } catch (error) {
    console.error(error);
  }
  return tracks;
};

export default fetchTracks;
